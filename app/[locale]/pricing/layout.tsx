import PlainPageShell from "@/components/PlainPageShell";

export default function PricingLayout({ children }: { children: React.ReactNode }) {
  return <PlainPageShell>{children}</PlainPageShell>;
}
